function x = createFullSinePulse(n,N,Fs,freqs,noise)
    x = zeros(N,n);
    for i = 1:n
        inSignal = 1;
        f = freq(i);
        indices = [randi(floor(Fs/f))];
        while inSignal
           indices(end+1) = 
           inSignal = (indices(end)+)
        end
    end
end